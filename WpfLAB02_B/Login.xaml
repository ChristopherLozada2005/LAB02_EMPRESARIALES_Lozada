<Window x:Class="WpfLAB02_B.Login"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfLAB02_B"
        mc:Ignorable="d"
        Title="Login" Height="450" Width="800">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Usuario -->
        <StackPanel Orientation="Horizontal" Margin="0,5">
            <Label Content="Usuario:" Width="70"/>
            <TextBox x:Name="txtUsuario" Width="180"/>
        </StackPanel>

        <!-- Contraseña -->
        <StackPanel Orientation="Horizontal" Margin="0,5" Grid.Row="1">
            <Label Content="Contraseña:" Width="70"/>
            <PasswordBox x:Name="txtPassword" Width="180"/>
        </StackPanel>

        <!-- Botón -->
        <Button Grid.Row="2" Content="Ingresar" Width="100" Margin="0,10,0,0" 
                HorizontalAlignment="Center" Click="BtnIngresar_Click"/>

        <!-- Mensaje -->
        <TextBlock x:Name="lblMensaje" Grid.Row="3" Foreground="Red" 
                   TextAlignment="Center" FontWeight="Bold"/>
    </Grid>
</Window>
